"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const mocha_1 = require("mocha");
const chai_1 = require("chai");
require("../src/core");
(0, mocha_1.describe)('Test client request', () => {
    (0, mocha_1.it)('fetch json and parse', async () => {
        const response = await request.get('https://jsonplaceholder.typicode.com/todos');
        chai_1.assert.equal(response.status, 200, 'failed to retrieve valid response');
        const todos = response.json();
        chai_1.assert.isArray(todos);
    });
});
